const mongoose = require('mongoose')
const dotenv = require('dotenv').config()


async function main () {
    try{

    } catch (error) {
        console.log('error', error)
    }
}

module.exports = main